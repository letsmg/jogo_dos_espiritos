version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: jogo_dos_espiritos_nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./public:/usr/share/nginx/html
    restart: unless-stopped
